<%= Timex.format!(@date, "%B %Y", :strftime) %> Calendar

<div class="calendar container">
  <%= for date <- dates(@date) do %>
    <div class="calendar date">
      <%= date.day %>
    </div>
  <% end %>
</div>
