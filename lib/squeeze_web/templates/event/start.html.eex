<h2>When do you want to start training?</h2>

<%= async_form @conn, event_path(@conn, :update, @step), fn f -> %>
  <div class="field">
    <%= text_input f, :start_at, class: 'date-picker' %>
  </div>

  <%= submit "Next", class: "primary button" %>
<% end %>
